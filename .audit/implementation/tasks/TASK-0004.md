# TASK-0004 — Activar HSTS Fase 1 (transición): max-age=604800 en .htaccess

## Status and scheduling
- Estado: **BLOCKED** (ADR 0020 — HSTS aplazado hasta post-WordPress + ≥30 días estables)
- Ola: WAVE-1 · Prioridad: 1 · Esfuerzo estimado: LT_30_MIN · Riesgo de regresión: LOW
- Creado por: audit-agent · Última actualización: 2026-07-19T23:00:00Z

## Objective
Servir `Strict-Transport-Security "max-age=604800"` (7 días, solo host) en las respuestas HTTPS del host principal — **Fase 1** del despliegue escalonado (ADR 0018, `.audit/hsts-decision.md`).

## Findings and evidence addressed
- Findings: SEC-001 (ver `../../findings.jsonl` y `../../remediation/`)
- Evidencia: EVID-0004, EVID-0005, EVID-0008, EVID-0011, EVID-0029 (ver `../../evidence-ledger.jsonl`)

## Why this task exists
Derivada de la auditoría 2026-07-19. La decisión vigente **no** es descomentar `max-age=31536000`: es activar HSTS ya con **max-age corto** durante la transición estático → WordPress; el año completo queda para **Fase 2** tras WordPress estable (ADR 0018).

## Definition of done
- Cabecera exacta `max-age=604800` presente en HTTPS 200/301/404 de apex y www
- Sin includeSubDomains ni preload
- Redirección HTTP intacta

## In scope
- Sustituir la línea comentada ~103 de `.htaccess` por la cabecera Fase 1 (ver Proposed technical changes)

## Out of scope
- Fase 2 (`max-age=31536000`) — post-corte WordPress + 30 días estables
- includeSubDomains (TASK-0012)
- preload (rechazado)
- Cualquier otra directiva

## Verified implementation locations
- .htaccess:103

## Allowed and protected paths
- Permitidos: .htaccess
- Protegidos (no tocar): todo lo demás

## Prerequisites
- Decisión ACTIVATE_HSTS_NOW + despliegue escalonado registrada en `hsts-decision.md` y ADR 0018 (hecho)

## Dependencies and blocked tasks
- Depende de: nada
- Bloquea a: TASK-0005, TASK-0008, TASK-0012

## Conflict group and parallel-execution rules
- Conflict group: `CG-HTACCESS` — las tareas del mismo grupo se ejecutan en serie.
- Paralelizable con: TASK-0001, TASK-0002
- Reclamar el lock del grupo en `locks/` antes de empezar.

## Recommended agent profile and owner
- Owner: DevOps · Perfil de agente: devops

## Ordered implementation procedure
1. Editar `.htaccess` ~103: activar cabecera Fase 1 (604800), no la de un año
2. Revisar diff (solo esa línea + comentario ADR si aplica)
3. Deploy
4. Ejecutar validación básica y dejar la verificación completa a TASK-0005

## Proposed technical changes
- - # Header always set Strict-Transport-Security "max-age=604800"
+ Header always set Strict-Transport-Security "max-age=604800"

(Si la línea comentada aún dice `31536000`, reemplazar por `604800`.)

**Alternativa ultra-conservadora (solo si ADR 0018 se interpreta así):** `max-age=300` o `86400` — documentar desviación en el artefacto de resultado.

**No preferido:** activar `max-age=31536000` en esta tarea salvo renuncia explícita al escalonado.

Todo cambio listado es PROPUESTO y NO EJECUTADO por la auditoría.

## Required tests
- Smoke test de las 13 páginas sobre HTTPS

## Acceptance criteria
- curl HTTPS apex/www/404/301 muestra exactamente `max-age=604800`
- curl -sS -D - -o /dev/null http://caminodeldharma.org/ sigue 301

## Deployment and production verification
- Secuencia: Deploy .htaccess, Verificación inmediata, TASK-0005 en sesión independiente
- Comandos de validación:
- curl -sS -D - -o /dev/null https://caminodeldharma.org/ | grep -i strict-transport-security
- curl -sS -D - -o /dev/null https://www.caminodeldharma.org/ | grep -i strict-transport-security
- curl -sS -D - -o /dev/null https://caminodeldharma.org/pagina-inexistente | grep -i strict-transport-security
- curl -sS -D - -o /dev/null https://caminodeldharma.org/eventos/ | grep -i strict-transport-security

## Monitoring
- Monitorización de caducidad del certificado (vence 2026-10-12)
- Recordatorio: planificar subida a `max-age=31536000` (Fase 2) tras WordPress estable — ADR 0018

## Rollback
- Disparadores: Fallo TLS, Ruptura atribuible a la cabecera
- Procedimiento: Servir Strict-Transport-Security "max-age=0" sobre HTTPS válido; NUNCA retirar la cabecera sin más. En Fase 1 la política caduca en ≤7 días en clientes que no revisiten tras max-age=0.

## Expected implementation result artifact
`implementation/results/TASK-0004.md` con: estado inicial/final del árbol, archivos cambiados, comandos y salidas, autochequeo de criterios, desviaciones, riesgos, notas de rollback y diff exacto.

## Independent validation checklist
- [ ] Estado de la tarea era IMPLEMENTED_PENDING_VALIDATION
- [ ] El diff se limita al alcance declarado (sin refactors oportunistas)
- [ ] Cada criterio de aceptación reproducido de forma independiente
- [ ] Comandos de validación re-ejecutados con salida registrada
- [ ] Valor HSTS = max-age=604800 (no 31536000)
- [ ] Sin regresiones en los journeys afectados
- [ ] Rollback sigue siendo viable
- [ ] Veredicto VALIDATED o CHANGES_REQUESTED escrito en `implementation/validations/TASK-0004.md`

## Blockers, decisions, and assumptions
- Blocker actual: ninguno
- Decisión: despliegue escalonado ADR 0018 (Fase 1 = 604800)

## Source freshness checks
Antes de editar: confirmar que el repo sigue en la rama main sin cambios que afecten a .htaccess:103 desde el commit auditado be896db2214c4dafdc8adad89f8496421c8b6071; si difiere, re-verificar la evidencia citada antes de continuar o marcar STALE.
